# ── Required ──────────────────────────────────────────────────────────────────
# LAN IP of the machine running this Docker (used for TLS cert SAN + RTP)
ASTERISK_IP=192.168.X.X

# ── PostgreSQL ────────────────────────────────────────────────────────────────
PG_HOST=127.0.0.1
PG_PORT=5432
PG_DB=asterisk_cdr
PG_USER=asterisk
PG_PASSWORD=asterisk123

# ── Asterisk AMI ──────────────────────────────────────────────────────────────
AMI_SECRET=tayyab

# ── Asterisk ARI ──────────────────────────────────────────────────────────────
ARI_PASSWORD=crm_ari_pass123

# ── CORS — frontend origins allowed to connect ────────────────────────────────
# Comma separated, no spaces
ALLOWED_ORIGINS=http://localhost:3000,https://app.leadspoint.in

# ── SSH Tunnel to AWS (optional) ──────────────────────────────────────────────
# Only needed if Django backend is on AWS and needs to reach local PostgreSQL/AMI
# To enable: docker compose --profile tunnel up -d
AWS_HOST=YOUR_AWS_IP
AWS_USER=ubuntu
AWS_KEY_PATH=/path/to/your/aws_key.pem
TUNNEL_PG_PORT=15432
TUNNEL_AMI_PORT=15038
TUNNEL_AGI_PORT=14573

# ── AGI Routing server ────────────────────────────────────────────────────────
# If Django backend is remote (tunnel mode): set to 127.0.0.1 and TUNNEL_AGI_PORT
# If Django backend is local: set to 127.0.0.1:4573
AGI_HOST=127.0.0.1
AGI_PORT=4573
